#include <iostream>
#include <conio.h>
using namespace std;
int main()
{
    cout << "Press v to end in C++ ";
    getch();// If we open this program in command promopt then it will wait for an input because of getch function after pressing any button it will just exit the function.
}